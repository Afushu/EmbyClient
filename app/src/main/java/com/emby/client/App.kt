package com.emby.client

import android.app.Application

class App : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialization (e.g., Room DB, Dependency Injection, SharedPreferences)
    }
}
